import { TRoutes } from "@/types"
const routes: TRoutes = {
    'signin': {
        path: '/auth/signin'
    },
    'signup': {
        path: '/auth/signup'
    },
    'dashboard':{
        path:"/dashboard"
    }
}
export default routes